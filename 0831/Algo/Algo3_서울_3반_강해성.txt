
# 코드의 순서는 처음에 학생1에서 하나씩 선택합니다
# 선택한 숫자로 부터 일단 다음 배열에서 같은 색깔은 제외하고 그 3개중에서 가장 작은 숫자를 선택합니다
# 계속해서 이런방식으로 다음 행렬에서 제일 낮은 숫자를 선택합니다

minValue = 0
minidx = 199
min = 199

for i in range(4):
    arr[0][i] < min
    minidx = i

for i in range(1, studentnumber):
    minValue += min(card[0])

    for j in range(4):
        if card[i][j] != card[i][minidx]:
            minValue += min(card[i][j])

